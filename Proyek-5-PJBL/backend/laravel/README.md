# Laravel Backend

Not yet implemented — see [`../../docs/IMPLEMENTATION_PLAN.md`](../../docs/IMPLEMENTATION_PLAN.md)
Phase 6, [`../../docs/API.md`](../../docs/API.md) for the frozen request/response contract, and
[`../../docs/DATABASE.md`](../../docs/DATABASE.md) for the schema.

Setup once scaffolding a real Laravel project here:

```bash
composer create-project laravel/laravel .
cp .env.example .env
php artisan key:generate
php artisan migrate
php artisan serve
```
